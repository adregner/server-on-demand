# If you are going to be working with xen.py on a non-xen-VM
# these environment variables will override any attempts the
# script makes to read/write to the xenbus.

export XENBUS_NAME=instance-666-777
export XENBUS_VM_DATA_HOSTNAME=serverondemand-277284732
export XENBUS_VM_DATA_PROVIDER_DATA_REGION=dfw
